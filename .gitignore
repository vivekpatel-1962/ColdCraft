# Secrets
.env
.env.local
.env.*.local
# Gmail OAuth: the client secret and (worse) the refresh token
credentials.json
.gmail_token.json

# Python
__pycache__/
*.pyc
.venv/
venv/

# Data (local SQLite + uploaded resumes) — anchored to the project root so it
# doesn't also swallow backend/app/data/ (real source files the pipeline needs).
/data/
*.db

# Node (frontend, later)
node_modules/
dist/
build/

# OS / editor
.DS_Store
Thumbs.db

# Personal / local-only (interview prep — not part of the project)
interview-prep/

# Claude Code project-local config (dev-server launch config, permission allowlist)
.claude/
