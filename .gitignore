# Secrets
.env
# Gmail OAuth: the client secret and (worse) the refresh token
credentials.json
.gmail_token.json

# Python
__pycache__/
*.pyc
.venv/
venv/

# Data (local SQLite + uploaded resumes)
data/
*.db

# Node (frontend, later)
node_modules/
dist/
build/

# OS / editor
.DS_Store
Thumbs.db

# Personal / local-only (interview prep — not part of the project)
interview-prep/
